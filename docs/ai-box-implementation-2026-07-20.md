# AI BOX — 구현 절차

기획: [`ai-box-plan-2026-07-20.md`](ai-box-plan-2026-07-20.md)

깃허브 MD 연동은 서버 없이 정적 앱만으로 가능하므로 1단계로 먼저 간다.
구글드라이브는 인증·CORS 제약이 있어 2단계로 분리한다.

---

## 1단계 — 깃허브 MD → 웹앱 자동 갱신

### 1-1. 글 저장소 구조 확정

레포에 카테고리당 MD 파일 하나. 파일명 = 카테고리.

```
posts/자유.md
posts/정보.md
posts/그림프롬프트.md
posts/페르소나프롬프트.md
```

### 1-2. 글 형식(포맷) 확정

AI가 쓰기 쉽고 앱이 파싱하기 쉬운 최소 규칙:

```markdown
## 글 제목 (2026-07-20)

본문. 여러 줄 가능, 코드 블록 가능.
```

- `## ` 헤딩 하나가 카드 하나
- 새 글은 파일 **맨 위**에 추가 (최신순)
- 헤딩 끝의 `(YYYY-MM-DD)`는 날짜 표기 — 없으면 날짜 없이 표시

### 1-3. 웹앱에서 읽기

- `https://raw.githubusercontent.com/LUNA-X59Q/LUNA-WORLD/main/posts/<카테고리>.md`
  를 `fetch` — raw는 CORS 허용이라 브라우저에서 바로 됨, API 키 불필요
- 파일이 없으면(404) 그 카테고리는 깃허브 글 0개로 처리
- 받은 MD를 `## ` 기준으로 잘라 카드로 렌더 (본문은 최소 마크다운:
  줄바꿈·코드블록·굵게 정도만, 외부 라이브러리 없이)
- 성공한 응답은 localStorage에 캐시 → 오프라인·실패 시 마지막 판 표시

주의: raw CDN 캐시 때문에 푸시 후 반영까지 최대 ~5분 지연될 수 있음.
그래서 **새로고침 버튼**을 두고, 앱 열 때마다 자동 fetch한다.

### 1-4. UI 통합

- 깃허브 글 카드에 출처 배지(예: `GitHub`) — 로컬 카드와 구분
- 깃허브 카드는 앱에서 삭제 불가(원본이 레포이므로), 로컬 카드만 삭제 가능
- 정렬: 깃허브 글(파일 순서 = 최신순) 먼저, 그 아래 로컬 카드 — 또는
  날짜 있으면 섞어서 최신순

### 1-5. 검증

- posts/ 파일에 샘플 글 1개 넣고 → 앱에서 카드로 보이는지
- AI(클로드 등)에게 "그림프롬프트에 글 하나 올려줘" 시켜서
  커밋 → 푸시 → 앱 새로고침 → 카드 등장까지 전체 흐름 확인
- 파일 없음(404)·형식 어긋난 글·오프라인 3가지 경우 확인

**1단계 끝나면 기획의 핵심("AI가 MD에 쓰면 앱에 뜬다")은 완성.**

---

## 2단계 — 구글드라이브 MD 읽기 (선택)

깃허브보다 제약이 많아 뒤로 뺀다. 두 가지 경로 중 택1:

- **A. Drive API + API 키**: 파일을 "링크가 있는 모든 사용자" 공개로 두고
  `www.googleapis.com/drive/v3/files/<ID>?alt=media&key=<API키>` fetch.
  googleapis.com은 CORS 허용이라 클라이언트에서 됨.
  단점: API 키가 HTML에 노출됨(읽기 전용 공개 파일이므로 위험은 낮음),
  구글 클라우드 콘솔에서 키 발급 필요
- **B. 경유 서버**: Cloud Run에 작은 프록시를 두고 서버가 Drive를 읽음.
  키가 숨겨지지만 "서버 0 유지" 운영 수칙과 충돌 → 웬만하면 A

절차는 1단계와 동일: 카테고리당 파일 1개(파일 ID 4개를 앱에 상수로 등록)
→ fetch → 같은 파서 재사용 → 출처 배지만 `Drive`로.

### 구현 상태 (A안으로 완료 — 설정값만 대기)

`index.html`에 드라이브 소스를 넣어 깃허브와 같은 파서·렌더·캐시를 공유하게
했다. 배지는 초록색 `Drive`. 다음 두 상수만 채우면 자동으로 켜진다(비면 꺼짐):

```js
const DRIVE_KEY='';   // 구글 클라우드 콘솔에서 발급한 API 키
const DRIVE_FILES={   // 카테고리 → 드라이브 파일 ID (모르는 칸은 비워둠)
  '자유':'', '정보':'', '그림프롬프트':'', '페르소나프롬프트':'',
};
```

**사용자가 준비할 것**

1. **API 키** — 구글 클라우드 콘솔에서 프로젝트 생성 →
   "API 및 서비스 → 라이브러리"에서 **Google Drive API** 사용 설정 →
   "사용자 인증 정보 → 사용자 인증 정보 만들기 → API 키". 노출을 줄이려면
   키 제한에서 "Drive API"로 API를 제한(선택).
2. **파일 ID** — 카테고리별 `.md` 파일을 드라이브에 올리고 각 파일을
   "링크가 있는 모든 사용자(뷰어)"로 공개 → 공유 링크
   `https://drive.google.com/file/d/<ID>/view`의 `<ID>` 부분을 위 상수에 입력.
   글 형식은 깃허브와 동일(`## 제목 (YYYY-MM-DD)`).

API 키는 읽기 전용 공개 파일에만 접근하므로 정적 HTML에 노출돼도 위험이 낮다.

---

## 순서 요약

| # | 할 일 | 산출물 |
|---|---|---|
| 1 | posts/ 폴더 + 카테고리 MD 4개 생성 (빈 파일 또는 샘플 1개) | 레포 커밋 |
| 2 | index.html에 fetch + MD 파서 + 깃허브 카드 렌더 | 앱 갱신 |
| 3 | 출처 배지·새로고침 버튼·오프라인 캐시 | 앱 갱신 |
| 4 | AI로 글 올리기 end-to-end 검증 | 확인 기록 |
| 5 | (선택) Drive 소스 코드 구현 — 키·파일 ID 상수만 대기 | 2단계 ✅ 코드 완료 |
